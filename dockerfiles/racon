#Base images
FROM ubuntu:18.04

#Constants
ARG RACON_VERSION=1.3.2

#Setup
RUN apt-get update \
 && apt-get install -y gcc\
                       g++ \
                       cmake \
                       make \
                       git

#Set Workdir
WORKDIR /software

#Install racon
RUN git clone --recursive https://github.com/isovic/racon.git racon
WORKDIR /software/racon
RUN git checkout ${RACON_VERSION}
WORKDIR /racon/racon/build
RUN cmake -DCMAKE_BUILD_TYPE=RELEASE ..
RUN make
ENV PATH="/racon/racon/build/bin:${PATH}"

#Cleanup
RUN apt-get clean
